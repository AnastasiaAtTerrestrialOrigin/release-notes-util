import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.terrestrialorigin.releasenotesutil',
  appName: 'release-notes-util',
  webDir: 'dist'
};

export default config;
